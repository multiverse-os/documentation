# Framekit: HTML/CSS/SVG GUI System 
A GUI system that allows re-using web application tools to build beautiful GUI
interfaces and specifically does not use javascript to reduce the security
implications.
